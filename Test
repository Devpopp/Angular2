<mat-grid-list cols="4" rowHeight="fit" gutterSize="10px" (window:resize)="onResize($event)">
  <mat-grid-tile *ngFor="let tile of tiles" [colspan]="tile.cols" [rowspan]="tile.rows">
    <mat-card>
      <div class="tile-content">
        <!-- Your fixed position content goes here -->
        <h3>{{ tile.text }}</h3>
      </div>
    </mat-card>
  </mat-grid-tile>
</mat-grid-list>
